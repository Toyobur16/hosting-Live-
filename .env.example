# Server-side Gemini API key (optional for AI features)
GEMINI_API_KEY=

# Email SMTP settings (Optional: for sending subscription & deposit alerts)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
